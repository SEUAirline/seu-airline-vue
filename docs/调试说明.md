# SEUAirline 调试说明

**最后更新:** 2025-11-09  
**适用版本:** v1.0+

---

## 📋 测试环境说明

### API 架构

本项目采用**前后端分离架构**，支持两种API模式：

#### 1. Mock API（开发/演示模式）
- **前缀:** `/mock/api/*`
- **用途:** 前端独立开发、功能演示
- **数据:** 内存中的模拟数据（刷新丢失）
- **配置:** `vite-plugin-mock` 拦截请求

#### 2. 真实后端API（集成/生产模式）
- **前缀:** `/api/*`
- **用途:** 完整的前后端集成
- **数据:** MySQL数据库持久化
- **配置:** Vite代理转发到 `http://localhost:8080`

### 测试数据来源

| 模式         | 数据来源                           | 初始数据                 |
| ------------ | ---------------------------------- | ------------------------ |
| **Mock API** | `mock/*.ts` 文件中的 `mockjs` 生成 | 随机生成（每次启动不同） |
| **真实API**  | MySQL数据库                        | `init-db.sql` 脚本初始化 |

---

## 🚀 启动前准备

### 使用Mock API测试

```powershell
# 1. 进入前端目录
cd seu-airline-vue

# 2. 启动开发服务器（Mock自动启用）
npm run dev
```

**访问:** `http://localhost:5173`

### 使用真实后端API测试

```powershell
# 1. 启动MySQL服务
# 确保MySQL已启动

# 2. 启动Redis服务
redis-server

# 3. 启动后端（在后端目录）
cd seu-airline-backend
mvn spring-boot:run

# 4. 启动前端（在前端目录）
cd seu-airline-vue
npm run dev
```

**访问:** `http://localhost:5173`

---

## 🧪 测试账户

### Mock API测试账户

```typescript
// mock/user.ts 中定义
用户名: user001
密码: 123456
```

### 真实后端测试账户

```sql
-- init-db.sql 中初始化

管理员账户:
用户名: admin
密码: admin123

普通用户:
用户名: user001
密码: 123456

普通用户2:
用户名: passenger1
密码: passenger123
```

---

## 🐛 常见问题与解决方案

### 问题 1: 机场数据加载失败

**现象:**
- 下拉框没有机场数据
- 控制台报错: "加载机场数据失败"

**排查步骤:**

1. **检查Mock API是否启用**
```bash
# 检查 .env.development
VITE_USE_MOCK=true

# 重启服务器
npm run dev
```

2. **查看控制台日志**
打开浏览器控制台 (F12)，查看是否有以下日志：
```
开始加载机场数据...
Flight Store: 调用 API 获取机场列表...
Flight Store: API 响应: {code: 200, message: "查询成功", data: Array(10), success: true}
```

3. **检查Network请求**
- 请求URL应该是: `/api/airport/list`
- 状态码应该是: 200
- 响应数据应该包含10个机场

**解决方案:**
- 确保Mock API已启用
- 重启开发服务器
- 清除浏览器缓存 (Ctrl+Shift+R)

---

### 问题 2: TypeScript 类型错误

**现象:**
- IDE显示类型错误
- 编译时报错

**常见错误:**

#### 错误 2.1: 支付方式类型不匹配
```typescript
// 错误
const selectedPaymentMethod = ref('alipay')

// 正确
const selectedPaymentMethod = ref<'alipay' | 'wechat' | 'bank'>('alipay')
```

#### 错误 2.2: 文件不在项目列表中
```json
// tsconfig.node.json
{
  "include": ["vite.config.ts", "mock-data.ts"]  // 添加所需文件
}
```

**解决方案:**
1. 添加明确的类型注解
2. 更新 `tsconfig.json` 配置
3. 运行类型检查: `npm run type-check`

---

### 问题 3: API 响应格式错误

**现象:**
- 数据无法正确显示
- 控制台显示 "API 响应不符合预期"

**检查要点:**

1. **响应格式必须包含 `success` 字段**
```typescript
// 正确的响应格式
{
  code: 200,
  message: "查询成功",
  success: true,  // ← 必须有这个字段
  data: [...]
}
```

2. **检查API路径**
```typescript
// 错误 - 路径重复
/api/api/airport/list

// 正确
/api/airport/list
```

**解决方案:**
- 确保Mock API返回正确的格式
- 检查 `baseURL` 配置
- 使用Axios拦截器统一处理响应

---

### 问题 4: 支付页面倒计时不工作

**现象:**
- 倒计时显示 00:00
- 倒计时不递减

**排查步骤:**

1. **检查订单信息是否正确传递**
```typescript
// 路由跳转时必须传递订单信息
router.push({
  name: 'payment',
  query: { orderId: order.id }
})
```

2. **检查倒计时逻辑**
```typescript
// 确保 setInterval 正确设置
const timer = setInterval(() => {
  remainingTime.value--
  if (remainingTime.value <= 0) {
    clearInterval(timer)
    handleTimeout()
  }
}, 1000)
```

**解决方案:**
- 确保订单信息正确传递
- 检查定时器是否正确清理
- 在组件卸载时清除定时器

---

### 问题 5: 订单列表为空

**现象:**
- 订单列表页面显示"暂无订单"
- 明明已经创建了订单

**排查步骤:**

1. **检查API调用**
```typescript
// 确保正确调用订单列表API
const response = await orderApi.getOrders({
  status: selectedStatus.value,
  page: currentPage.value,
  pageSize: 10
})
```

2. **检查Mock数据**
```typescript
// mock/order.ts 中确保有订单数据
{
  url: '/api/orders',
  method: 'get',
  response: () => {
    return {
      code: 200,
      success: true,
      data: {
        list: [...], // 确保有数据
        total: 10
      }
    }
  }
}
```

**解决方案:**
- 检查API路径是否正确
- 确保Mock数据中有订单
- 检查筛选条件是否过滤掉了所有订单

---

### 问题 6: 路由跳转失败

**现象:**
- 点击按钮没有反应
- 控制台报错: "No match found for location"

**排查步骤:**

1. **检查路由配置**
```typescript
// router/index.ts
{
  path: '/orders',
  name: 'orders',  // 确保name正确
  component: () => import('@/views/user/OrdersPage.vue')
}
```

2. **检查路由跳转代码**
```typescript
// 使用name跳转
router.push({ name: 'orders' })

// 或使用path跳转
router.push('/orders')
```

**解决方案:**
- 确保路由已正确配置
- 使用正确的路由名称或路径
- 检查路由守卫是否阻止了跳转

---

## 🔍 调试技巧

### 1. 检查当前使用的API模式

打开浏览器控制台，查看Network标签：

```
Mock API模式:
- 请求URL包含 /mock/api/*
- 响应非常快（本地拦截）
- 数据每次可能不同

真实API模式:
- 请求URL为 /api/*
- Vite代理到 localhost:8080
- 数据来自数据库（稳定）
```

### 2. 查看网络请求

```javascript
// 在控制台执行，测试航班搜索

// Mock API
fetch('/mock/api/flight/search?departureCity=北京&arrivalCity=上海&departureDate=2025-11-01')
  .then(res => res.json())
  .then(data => console.log('Mock数据:', data))

// 真实API
fetch('/api/flight/search?departureCity=北京&arrivalCity=上海&departureDate=2025-11-01')
  .then(res => res.json())
  .then(data => console.log('真实数据:', data))
```

### 3. 调试Vue组件状态

```javascript
// 在控制台执行

// 查看Store状态
const flightStore = window.$pinia?.state.value.flight
console.log('航班Store:', flightStore)

// 查看路由信息
console.log('当前路由:', window.$router.currentRoute.value)
```

### 4. 使用Vue DevTools

1. 安装Vue DevTools浏览器扩展
2. 打开开发者工具中的Vue标签
3. 查看组件树、状态、事件等

### 5. 添加调试日志

```typescript
// 在关键位置添加日志
console.log('=== 开始处理订单 ===')
console.log('订单数据:', orderData)
console.log('用户信息:', userInfo)
console.log('=== 处理完成 ===')
```

---

## 📊 完整的数据流

### 机场数据加载流程

```
1. HomePage.vue onMounted
   ↓
2. flightStore.loadAirports()
   ↓
3. flightApi.getAirports()
   ↓
4. request.get('/airport/list')
   ↓
5. axios 请求: GET /api/airport/list
   ↓
6. Mock 插件拦截
   ↓
7. mock/flight.ts 返回数据
   ↓
8. axios 拦截器返回 response.data
   ↓
9. Store 保存到 airports.value
   ↓
10. HomePage 提取城市列表
   ↓
11. 下拉框显示城市
```

### 订单创建流程

```
1. FlightBookPage 填写信息
   ↓
2. 点击"提交订单"
   ↓
3. orderApi.createOrder(orderData)
   ↓
4. POST /api/orders
   ↓
5. Mock API 创建订单
   ↓
6. 返回订单信息
   ↓
7. 跳转到支付页面
   ↓
8. PaymentPage 显示订单详情
   ↓
9. 选择支付方式
   ↓
10. 提交支付
   ↓
11. 跳转到订单成功页
```

---

## ✅ 测试清单

### 功能测试

- [ ] **航班搜索**
  - [ ] 搜索结果正确显示
  - [ ] 筛选功能正常
  - [ ] 排序功能正常
  - [ ] 分页功能正常

- [ ] **航班预订**
  - [ ] 乘客信息填写
  - [ ] 附加服务选择
  - [ ] 价格计算正确
  - [ ] 订单创建成功

- [ ] **支付流程**
  - [ ] 订单信息显示正确
  - [ ] 支付方式选择
  - [ ] 倒计时正常
  - [ ] 支付成功跳转

- [ ] **订单管理**
  - [ ] 订单列表显示
  - [ ] 订单筛选
  - [ ] 订单搜索
  - [ ] 订单详情查看
  - [ ] 订单操作（取消/退票）

- [ ] **用户中心**
  - [ ] 个人信息显示
  - [ ] 信息编辑
  - [ ] 头像上传
  - [ ] 常用旅客管理
  - [ ] 账号设置

### 兼容性测试

- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Edge 浏览器
- [ ] Safari 浏览器（Mac）

### 响应式测试

- [ ] 桌面端 (1920x1080)
- [ ] 笔记本 (1366x768)
- [ ] 平板 (768x1024)
- [ ] 手机 (375x667)

---

## 📝 测试报告模板

```markdown
## 测试报告

**测试人员:** [姓名]
**测试时间:** 2025-11-09
**测试模式:** [ ] Mock API  [ ] 真实API
**浏览器:** Chrome / Firefox / Edge

### 测试环境
- 前端: http://localhost:5173 - [√] 正常
- 后端: http://localhost:8080 - [√] 正常（真实API模式）
- 数据库: MySQL - [√] 正常（真实API模式）

### 测试结果
| 功能     | Mock API          | 真实API           | 备注 |
| -------- | ----------------- | ----------------- | ---- |
| 航班搜索 | [ ] Pass [ ] Fail | [ ] Pass [ ] Fail |      |
| 航班预订 | [ ] Pass [ ] Fail | [ ] Pass [ ] Fail |      |
| 支付流程 | [ ] Pass [ ] Fail | [ ] Pass [ ] Fail |      |
| 订单管理 | [ ] Pass [ ] Fail | [ ] Pass [ ] Fail |      |
| 用户中心 | [ ] Pass [ ] Fail | [ ] Pass [ ] Fail |      |

### 发现的问题
1. [问题描述]
2. [问题描述]

### 截图
[粘贴截图]
```

---

## 🎯 调试建议

### 开发阶段
1. **使用Mock API** - 不依赖后端，快速开发
2. **添加详细日志** - 便于追踪问题
3. **使用Vue DevTools** - 实时查看组件状态
4. **分步测试** - 每完成一个功能就测试

### 联调阶段
1. **切换到真实API** - 测试完整流程
2. **检查数据格式** - 确保前后端一致
3. **处理异常情况** - 网络错误、超时等
4. **性能测试** - 检查响应速度

### 上线前
1. **全面测试** - 所有功能都要测试
2. **兼容性测试** - 多浏览器测试
3. **压力测试** - 模拟高并发
4. **安全测试** - 检查安全漏洞

---

## 💡 最佳实践

### 1. 错误处理
```typescript
try {
  const response = await api.getData()
  // 处理数据
} catch (error) {
  console.error('获取数据失败:', error)
  ElMessage.error('获取数据失败，请稍后重试')
}
```

### 2. 加载状态
```typescript
const loading = ref(false)

async function loadData() {
  loading.value = true
  try {
    const data = await api.getData()
    // 处理数据
  } finally {
    loading.value = false
  }
}
```

### 3. 防抖节流
```typescript
import { debounce } from 'lodash-es'

const handleSearch = debounce((keyword: string) => {
  // 搜索逻辑
}, 300)
```

### 4. 数据验证
```typescript
// 使用Zod或Yup进行数据验证
import { z } from 'zod'

const orderSchema = z.object({
  flightId: z.string(),
  passengers: z.array(z.object({
    name: z.string(),
    idCard: z.string().length(18)
  }))
})
```

---

**文档维护者:** SEUAirline 开发团队  
**最后更新:** 2025-11-09  
**如有问题，请查阅本文档或联系开发团队**
