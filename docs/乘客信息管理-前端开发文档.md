# 乘客信息管理 - 前端开发文档

> **项目根目录**: `SEUAirline/`  
> **前端项目**: `seu-airline-vue/`  
> **后端项目**: `seu-airline-backend/`

---

## 📋 目录

1. [功能概述](#功能概述)
2. [快速开始](#快速开始)
3. [代码结构](#代码结构)
4. [API 接口](#api-接口)
5. [类型定义](#类型定义)
6. [状态管理](#状态管理)
7. [使用示例](#使用示例)
8. [测试指南](#测试指南)

---

## 功能概述

### 业务需求

用户在订票时可以添加多个乘客，系统支持保存**常用乘客信息**，方便用户快速填写订票信息。

### 核心功能

- ✅ 查看常用乘客列表
- ✅ 添加新的常用乘客
- ✅ 编辑乘客信息
- ✅ 删除乘客信息
- ✅ 设置默认乘客
- ✅ 订票时自动填充乘客信息

---

## 快速开始

### 1. 前置条件

确保后端已完成以下配置：
- ✅ 执行数据库脚本 `seu-airline-backend/src/main/resources/init-database.sql`
- ✅ 后端服务正常运行（`http://localhost:8080`）
- ✅ Redis 服务已启动

### 2. 启动前端

```bash
cd seu-airline-vue
npm install
npm run dev
```

### 3. 测试账号

| 用户名 | 密码 | 角色 | 已有乘客数 |
|--------|------|------|-----------|
| `passenger1` | `password` | 乘客 | 3 人 |
| `passenger2` | `password` | 乘客 | 2 人 |

---

## 代码结构

### 文件清单

```
seu-airline-vue/
├── src/
│   ├── types/
│   │   └── passenger.ts           # 类型定义和转换函数
│   ├── api/
│   │   └── passenger.ts           # API 接口封装
│   ├── stores/
│   │   └── passenger.ts           # Pinia 状态管理
│   └── views/
│       ├── PassengersPage.vue     # 乘客管理页面（已存在）
│       └── FlightBookPage.vue     # 订票页面（已存在）
```

### 已创建的核心文件

#### 1. `types/passenger.ts` - 类型定义

定义了前后端数据格式，包括：
- `PassengerDisplay` - 前端显示格式
- `PassengerBackend` - 后端存储格式
- `toPassengerDisplay()` - 后端→前端转换
- `toBackendRequest()` - 前端→后端转换

#### 2. `api/passenger.ts` - API 接口

封装了完整的 CRUD 操作：
- `getPassengers()` - 获取乘客列表
- `getPassengerById()` - 获取单个乘客
- `createPassenger()` - 创建乘客
- `updatePassenger()` - 更新乘客
- `deletePassenger()` - 删除乘客
- `setDefaultPassenger()` - 设置默认乘客

#### 3. `stores/passenger.ts` - 状态管理

提供了完整的状态和操作：
- `frequentPassengers` - 乘客列表状态
- `loading` - 加载状态
- `fetchPassengers()` - 加载乘客列表
- `addPassenger()` - 添加乘客
- `updatePassenger()` - 更新乘客
- `removePassenger()` - 删除乘客

---

## API 接口

### 基础路径

```
http://localhost:8080/api/passengers
```

### 接口列表

| 方法 | 路径 | 功能 | 是否需要登录 |
|------|------|------|-------------|
| GET | `/` | 获取乘客列表 | ✅ |
| GET | `/{id}` | 获取乘客详情 | ✅ |
| POST | `/` | 创建乘客 | ✅ |
| PUT | `/{id}` | 更新乘客 | ✅ |
| PATCH | `/{id}/default` | 设置默认乘客 | ✅ |
| DELETE | `/{id}` | 删除乘客 | ✅ |

### 请求/响应示例

#### 创建乘客

**请求：**
```json
POST /api/passengers
{
  "passengerName": "张三",
  "idType": "ID_CARD",
  "idCard": "320106199001011234",
  "phone": "13800138000",
  "email": "zhangsan@example.com",
  "passengerType": "ADULT",
  "isDefault": true
}
```

**响应：**
```json
{
  "success": true,
  "message": "乘客创建成功",
  "data": {
    "id": 1,
    "passengerName": "张三",
    "idType": "ID_CARD",
    "idCard": "320106199001011234",
    "phone": "13800138000",
    "email": "zhangsan@example.com",
    "passengerType": "ADULT",
    "isDefault": true,
    "createdAt": "2025-11-12T10:30:00",
    "updatedAt": "2025-11-12T10:30:00"
  }
}
```

---

## 类型定义

### 前端显示格式

```typescript
interface PassengerDisplay {
  id?: number
  name: string              // 乘客姓名
  idType: IdType           // 证件类型
  idNumber: string         // 证件号码
  phone?: string           // 联系电话
  email?: string           // 邮箱地址
  passengerType: PassengerType  // 乘客类型
  isDefault?: boolean      // 是否默认
}
```

### 后端存储格式

```typescript
interface PassengerBackend {
  id?: number
  passengerName: string    // 后端用 passengerName
  idType: string
  idCard: string           // 后端用 idCard
  phone?: string
  email?: string
  passengerType: string
  isDefault?: boolean
  createdAt?: string
  updatedAt?: string
}
```

### 枚举类型

```typescript
// 证件类型
type IdType = 'ID_CARD' | 'PASSPORT' | 'OTHER'

// 乘客类型
type PassengerType = 'ADULT' | 'CHILD' | 'INFANT'
```

### 数据转换

前后端字段名不同，需要转换：

```typescript
// 后端 → 前端
const display = toPassengerDisplay(backendData)

// 前端 → 后端
const request = toBackendRequest(displayData)
```

---

## 状态管理

### 使用 Pinia Store

```typescript
import { usePassengerStore } from '@/stores/passenger'

const passengerStore = usePassengerStore()

// 加载乘客列表
await passengerStore.fetchPassengers()

// 获取乘客列表
const passengers = passengerStore.frequentPassengers

// 获取默认乘客
const defaultPassenger = passengerStore.defaultPassenger

// 添加乘客
await passengerStore.addPassenger(passengerData)

// 更新乘客
await passengerStore.updatePassenger(id, updatedData)

// 删除乘客
await passengerStore.removePassenger(id)

// 设置默认乘客
await passengerStore.setAsDefault(id)
```

---

## 使用示例

### 示例 1：在订票页面加载乘客列表

```vue
<script setup lang="ts">
import { onMounted } from 'vue'
import { usePassengerStore } from '@/stores/passenger'

const passengerStore = usePassengerStore()

onMounted(async () => {
  await passengerStore.fetchPassengers()
})
</script>

<template>
  <div>
    <h3>选择常用乘客</h3>
    <div v-if="passengerStore.loading">加载中...</div>
    <div v-else>
      <div 
        v-for="passenger in passengerStore.frequentPassengers" 
        :key="passenger.id"
        @click="selectPassenger(passenger)"
      >
        {{ passenger.name }} - {{ passenger.idNumber }}
        <span v-if="passenger.isDefault">（默认）</span>
      </div>
    </div>
  </div>
</template>
```

### 示例 2：添加新乘客

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { usePassengerStore } from '@/stores/passenger'
import type { PassengerDisplay } from '@/types/passenger'

const passengerStore = usePassengerStore()
const newPassenger = ref<PassengerDisplay>({
  name: '',
  idType: 'ID_CARD',
  idNumber: '',
  phone: '',
  email: '',
  passengerType: 'ADULT',
  isDefault: false
})

async function handleSubmit() {
  try {
    await passengerStore.addPassenger(newPassenger.value)
    alert('添加成功！')
  } catch (error) {
    alert('添加失败：' + error.message)
  }
}
</script>

<template>
  <form @submit.prevent="handleSubmit">
    <input v-model="newPassenger.name" placeholder="姓名" required />
    <select v-model="newPassenger.idType">
      <option value="ID_CARD">身份证</option>
      <option value="PASSPORT">护照</option>
      <option value="OTHER">其他</option>
    </select>
    <input v-model="newPassenger.idNumber" placeholder="证件号码" required />
    <input v-model="newPassenger.phone" placeholder="手机号" />
    <input v-model="newPassenger.email" placeholder="邮箱" type="email" />
    <select v-model="newPassenger.passengerType">
      <option value="ADULT">成人</option>
      <option value="CHILD">儿童</option>
      <option value="INFANT">婴儿</option>
    </select>
    <label>
      <input type="checkbox" v-model="newPassenger.isDefault" />
      设为默认乘客
    </label>
    <button type="submit">添加</button>
  </form>
</template>
```

### 示例 3：订票时自动填充乘客信息

```vue
<script setup lang="ts">
import { watch } from 'vue'
import { usePassengerStore } from '@/stores/passenger'

const passengerStore = usePassengerStore()
const selectedPassengerId = ref<number>()
const bookingForm = ref({
  passengerName: '',
  idCard: '',
  phone: '',
  // ... 其他字段
})

// 监听选择的乘客，自动填充表单
watch(selectedPassengerId, (passengerId) => {
  if (!passengerId) return
  
  const passenger = passengerStore.frequentPassengers.find(
    p => p.id === passengerId
  )
  
  if (passenger) {
    bookingForm.value.passengerName = passenger.name
    bookingForm.value.idCard = passenger.idNumber
    bookingForm.value.phone = passenger.phone || ''
  }
})
</script>
```

---

## 测试指南

### 功能测试清单

#### 1. 乘客列表页面测试

- [ ] 页面加载时自动获取乘客列表
- [ ] 默认乘客显示在列表顶部
- [ ] 按创建时间倒序排列
- [ ] 显示乘客的所有信息（姓名、证件类型、证件号等）

#### 2. 添加乘客测试

- [ ] 必填字段验证（姓名、证件类型、证件号、乘客类型）
- [ ] 手机号格式验证（11位，1开头）
- [ ] 邮箱格式验证
- [ ] 证件号重复检查
- [ ] 设置为默认乘客时，自动取消其他默认乘客
- [ ] 添加成功后列表自动刷新

#### 3. 编辑乘客测试

- [ ] 可以修改所有字段
- [ ] 修改证件号时检查重复
- [ ] 更新成功后列表自动刷新

#### 4. 删除乘客测试

- [ ] 删除前显示确认对话框
- [ ] 删除成功后从列表中移除
- [ ] 删除默认乘客后，不会自动设置新的默认乘客

#### 5. 设置默认乘客测试

- [ ] 点击"设为默认"按钮后，该乘客标记为默认
- [ ] 之前的默认乘客自动取消默认标记
- [ ] 每个用户只能有一个默认乘客

#### 6. 订票集成测试

- [ ] 订票页面可以选择常用乘客
- [ ] 选择后自动填充乘客信息
- [ ] 可以手动修改自动填充的信息
- [ ] 订票成功后，订单中保存了正确的乘客信息

### 测试步骤

#### 步骤 1: 登录系统

1. 打开浏览器访问 `http://localhost:5173`
2. 使用测试账号登录：`passenger1` / `password`

#### 步骤 2: 查看常用乘客

1. 导航到"常用乘客"页面
2. 验证列表显示3个乘客（张三、李四、王小明）
3. 验证"张三"标记为默认乘客

#### 步骤 3: 添加乘客

1. 点击"添加乘客"按钮
2. 填写表单：
   - 姓名：赵六
   - 证件类型：护照
   - 证件号：E12345678
   - 手机：13800138888
   - 邮箱：zhaoliu@test.com
   - 乘客类型：成人
3. 提交表单
4. 验证列表中出现新乘客

#### 步骤 4: 编辑乘客

1. 点击某个乘客的"编辑"按钮
2. 修改手机号
3. 保存修改
4. 验证修改已生效

#### 步骤 5: 设置默认乘客

1. 选择一个非默认乘客
2. 点击"设为默认"
3. 验证该乘客标记为默认
4. 验证之前的默认乘客取消了默认标记

#### 步骤 6: 删除乘客

1. 点击某个乘客的"删除"按钮
2. 确认删除
3. 验证该乘客从列表中消失

#### 步骤 7: 订票时使用常用乘客

1. 导航到"预订航班"页面
2. 选择一个航班
3. 在乘客信息表单中，从下拉列表选择常用乘客
4. 验证乘客信息自动填充
5. 完成订票流程

---

## 常见问题

### Q1: 乘客列表为空？

**解决方案：**
1. 检查是否已登录
2. 检查后端服务是否运行
3. 检查数据库是否执行了初始化脚本
4. 打开浏览器控制台查看 API 请求是否成功

### Q2: 创建乘客失败？

**常见原因：**
- 证件号码已存在（后端会检查重复）
- 手机号格式不正确（必须是11位，1开头）
- 邮箱格式不正确
- 乘客数量超过限制（每个用户最多20个）

### Q3: 前后端字段对应关系？

| 前端字段 | 后端字段 | 说明 |
|---------|---------|------|
| `name` | `passengerName` | 乘客姓名 |
| `idNumber` | `idCard` | 证件号码 |
| `idType` | `idType` | 证件类型 |
| `phone` | `phone` | 联系电话 |
| `email` | `email` | 邮箱地址 |
| `passengerType` | `passengerType` | 乘客类型 |
| `isDefault` | `isDefault` | 是否默认 |

**注意：** 使用 `types/passenger.ts` 中的转换函数自动处理字段映射！

### Q4: 如何在现有页面中集成？

如果你的页面已经实现了乘客管理功能，可以：

**选项1：使用新的 API 和 Store**
```typescript
// 替换原有的 API 调用
import { usePassengerStore } from '@/stores/passenger'
const passengerStore = usePassengerStore()
await passengerStore.fetchPassengers()
```

**选项2：保持现有实现**
- 新的 `api/passenger.ts` 提供了兼容层
- 包含了 deprecated 方法以支持旧代码
- 建议逐步迁移到新 API

---

## 技术细节

### 为什么需要字段转换？

前端使用 `name` 和 `idNumber`，后端使用 `passengerName` 和 `idCard`。这是因为：
- 前端遵循简洁命名原则
- 后端遵循数据库命名规范
- 转换层统一处理，避免业务代码中的重复转换

### 状态管理设计

使用 Pinia 的原因：
- ✅ 统一的数据源（避免多个组件重复请求）
- ✅ 响应式更新（列表变化自动同步到所有使用的组件）
- ✅ 类型安全（完整的 TypeScript 支持）
- ✅ 开发工具支持（Pinia DevTools）

### API 设计原则

- 所有方法自动处理前后端字段转换
- 统一的错误处理
- 提供兼容的 deprecated 方法
- 符合 RESTful 规范

---

## 代码规范

### 命名规范

- **类型名**：大驼峰（`PassengerDisplay`）
- **变量名**：小驼峰（`passengerStore`）
- **常量名**：全大写+下划线（`MAX_PASSENGERS`）
- **文件名**：小写+连字符（`passenger.ts`）

### 注释规范

```typescript
/**
 * 获取当前用户的所有常用乘客
 * @returns Promise<PassengerDisplay[]> 乘客列表
 */
async function getPassengers(): Promise<PassengerDisplay[]> {
  // 实现代码
}
```

---

## 相关文档

### 后端文档
- `seu-airline-backend/docs/常用乘客功能-后端实现说明.md`
- `seu-airline-backend/docs/数据库更新说明-乘客表.md`

### API 测试
- Swagger UI: `http://localhost:8080/swagger-ui.html`
- 查找 "乘客信息管理" 分组

---

**最后更新：** 2025年11月12日
**维护者：** SEUAirline 开发团队
